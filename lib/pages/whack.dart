import 'package:flutter/material.dart';

class WhackPage extends StatelessWidget {
  const WhackPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("WikiWhack")),
    );
  }
}
